name: auto_validating_text_field
description: To validate text field as soon as its value changes.
version: 0.0.1
homepage: https://github.com/auto_validating_text_field.git

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.20.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
